# yaml-language-server: $schema=../../../../../../../scripts/data_schema/example_description_schema.json
coex_edgefast_zigbee_router:
  section-type: application
  contents:
    meta_path: examples/coex_examples/coex_zigbee/router/freertos
    project-root-path: boards/${board}/coex_examples/coex_zigbee/zigbee/router/freertos
    document:
      name: coex_edgefast_zigbee_router
      category: coex_examples/coex_zigbee/zigbee/router/freertos
      brief: The Zigbee Router example is a simple router example on the MCUXpresso SDK.
        The application implements a Zigbee router that will attempt to connect to a Zigbee
        coordinator.
  boards:
    frdmrw612:
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    - -mdk@debug
    - -mdk@flash_debug
    - -mdk@flash_release
    - -mdk@release
    rdrw612bga:
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    - -mdk@debug
    - -mdk@flash_debug
    - -mdk@flash_release
    - -mdk@release

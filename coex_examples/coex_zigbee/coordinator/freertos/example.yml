# yaml-language-server: $schema=../../../../../../../scripts/data_schema/example_description_schema.json
coex_zigbee_coordinator:
  skip_build: true
  section-type: application
  contents:
    meta_path: examples/coex_examples/coex_zigbee/coordinator/freertos
    project-root-path: boards/${board}/coex_examples/coex_zigbee/coordinator/freertos
    document:
      name: coex_zigbee_coordinator
      category: coex_examples/coex_zigbee/coordinator/freertos
      brief: The coex zigbee Coordinator example is a simple Coex-coordinator example on the MCUXpresso SDK.
             The application implements a Coex-Zigbee coordinator that can form a network and steer it.
  boards:
    frdmrw612:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    - -mdk@debug
    - -mdk@flash_debug
    - -mdk@flash_release
    - -mdk@release
    rdrw612bga:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    - -mdk@debug
    - -mdk@flash_debug
    - -mdk@flash_release
    - -mdk@release

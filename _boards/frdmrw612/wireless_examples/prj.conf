# mbedtls config
CONFIG_MCUX_COMPONENT_middleware.wireless.framework.platform.mbedtls_config=y

# board config
# we don't really need board_comp and board_lp extensions but we enable them to track build issues
CONFIG_MCUX_PRJSEG_module.board.wireless.board_comp=y
CONFIG_MCUX_PRJSEG_module.board.wireless.board_lp=y
CONFIG_MCUX_PRJSEG_module.board.wireless.app_services=y

CONFIG_MBEDTLS_USER_CONFIG_ENABLE=y
CONFIG_MBEDTLS_USER_CONFIG_FILE="\"SecLib_mbedtls_config.h\""
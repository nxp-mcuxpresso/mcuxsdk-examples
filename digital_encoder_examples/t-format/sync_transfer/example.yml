t-format_sync_transfer:
  section-type: application
  contents:
    meta_path: examples/digital_encoder_examples/t-format/sync_transfer
    project-root-path: boards/${board}/digital_encoder_examples/t-format/sync_transfer/${multicore_foldername}
    document:
      name: t-format_sync_transfer${core_id_suffix_name}
      category: digital_encoder_examples
      brief: The t-format_sync example shows how to use flexio T-format driver
        in sync way:In this example, a flexio simulated T-format connect to the encoder
        through RS-485, the board will send requests to and receive responses from the encoder.
      example_readme:
      - examples/digital_encoder_examples/t-format/sync_transfer/readme.md
      - ${board_root}/${board}/digital_encoder_examples/t-format/sync_transfer/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm33_core1:
    - +armgcc@debug
    - +armgcc@release

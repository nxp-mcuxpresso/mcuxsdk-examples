endat2p2:
  section-type: application
  contents:
    meta_path: examples/digital_encoder_examples/endat2p2
    project-root-path: boards/${board}/digital_encoder_examples/endat2p2/${multicore_foldername}
    document:
      name: endat2p2${core_id_suffix_name}
      category: digital_encoder
      brief: EnDat2.2 interface example
      example_readme:
      - examples/digital_encoder_examples/endat2p2/readme.md
      - ${board_root}/${board}/digital_encoder_examples/endat2p2/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm7_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release
    imx943evk@cm7_core0:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release
    imx943evk@cm33_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release
    imx943evk:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug

# yaml-language-server: $schema=../../../../../../../scripts/data_schema/example_description_schema.json
zigbee_router_bm_r23:
  section-type: application
  contents:
    meta_path: examples/wireless_examples/zigbee/router/bm/r23
    project-root-path: boards/${board}/wireless_examples/zigbee/router_r23/bm
    document:
      name: zigbee_router_bm_r23
      category: wireless_examples/zigbee/router/bm
      brief: The Zigbee Router example is a simple router example on the MCUXpresso SDK.
        The application implements a Zigbee router that will attempt to connect to a Zigbee
        coordinator.
  boards:
    k32w148evk:
    - +armgcc@debug
    - +armgcc@release
    - -iar@debug
    - -iar@release
    frdmmcxw71:
    - +armgcc@debug
    - +armgcc@release
    - -iar@debug
    - -iar@release

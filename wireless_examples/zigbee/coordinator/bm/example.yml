# yaml-language-server: $schema=../../../../../../../scripts/data_schema/example_description_schema.json
zigbee_coordinator_bm:
  section-type: application
  contents:
    meta_path: examples/wireless_examples/zigbee/coordinator/bm
    project-root-path: boards/${board}/wireless_examples/zigbee/coordinator/bm
    document:
      name: zigbee_coordinator_bm
      category: wireless_examples/zigbee/coordinator/bm
      brief: The zigbee Coordinator example is a simple coordinator example on the MCUXpresso SDK.
             The application implements a Zigbee coordinator that can form a network and steer it.
  boards:
    #frdmmcxw71:
    #- +armgcc@debug
    #- +armgcc@release
    #- -iar@debug
    #- -iar@release
    #mcxw71evk:
    #- +armgcc@debug
    #- +armgcc@release
    #- -iar@debug
    #- -iar@release

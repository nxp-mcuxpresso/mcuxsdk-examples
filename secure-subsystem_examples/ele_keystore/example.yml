# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
ele_keystore:
  section-type: application
  contents:
    meta_path: examples/secure-subsystem_examples/ele_keystore
    project-root-path: boards/${board}/secure-subsystem_examples/ele_keystore/${multicore_foldername}
    document:
      name: ele_keystore${core_id_suffix_name}
      category: secure-subsystem_examples
      brief: The ELE keystore application provides example how to use SSSAPI to export key
        into blob and later import it back.
      example_readme:
      - examples/secure-subsystem_examples/ele_keystore/readme.md
      - ${board_root}/${board}/secure-subsystem_examples/ele_keystore/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    kw45b41zevk:
    - +armgcc@debug
    - +armgcc@release
    k32w148evk:
    - +armgcc@debug
    - +armgcc@release
    frdmmcxw71:
    - +armgcc@debug
    - +armgcc@release
    mcxw71evk:
    - +armgcc@debug
    - +armgcc@release
    mcxw72evk@cm33_core0:
    - +armgcc@debug
    - +armgcc@release
    mcxw72loc@cm33_core0:
    - +armgcc@debug
    - +armgcc@release
    kw47evk@cm33_core0:
    - +armgcc@debug
    - +armgcc@release
    kw47loc@cm33_core0:
    - +armgcc@debug
    - +armgcc@release

# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
hsr_switch:
  section-type: application
  contents:
    meta_path: examples/driver_examples/netc/hsr_switch
    project-root-path: boards/${board}/driver_examples/netc/hsr_switch/${multicore_foldername}
    document:
      name: hsr_switch${core_id_suffix_name}
      category: driver_examples/netc
      brief: 'The example shows the way to use NETC driver to act as a HSR switch. '
      example_readme:
      - examples/driver_examples/netc/hsr_switch/readme.md
      - ${board_root}/${board}/driver_examples/netc/hsr_switch/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm7_core1:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug
    imx943evk@cm7_core0:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug
    imx943evk@cm33_core1:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug

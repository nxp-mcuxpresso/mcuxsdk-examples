
config secondary_board
    string
    default "$(board)"

config secondary_toolchain
    string
    default "$(toolchain)"

source "${SdkRootDirPath}/examples/_boards/${board}/driver_examples/netc/psi_vsi_message/${core_id}/Kconfig.sysbuild"
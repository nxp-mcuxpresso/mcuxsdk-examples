lpit_single_channel_trigger:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpit/single_channel_trigger
    project-root-path: boards/${board}/driver_examples/lpit/single_channel_trigger/${multicore_foldername}
    document:
      name: lpit_single_channel_trigger${core_id_suffix_name}
      category: driver_examples/lpit
      brief: The LPIT single channel project is a simple example of the SDK LPIT driver.
        It sets up the LPIThardware block to trigger a periodic interrupt after every
        1 second. When the LPIT interrupt is triggereda message a printed on the serial
        terminal and an LED is toggled on the board.
      example_readme:
      - examples/driver_examples/lpit/single_channel_trigger/readme.md
      - examples/_boards/${board}/driver_examples/lpit/single_channel_trigger/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm33_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release

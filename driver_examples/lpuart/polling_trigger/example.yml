lpuart_polling_trigger:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpuart/polling_trigger
    project-root-path: boards/${board}/driver_examples/lpuart/polling_trigger/${multicore_foldername}
    document:
      name: lpuart_polling_trigger${core_id_suffix_name}
      category: driver_examples/lpuart
      brief: The lpuart_polling_trigger Example project is to demonstrate usage of
        the KSDK lpuart driver.In the example, you can send characters to the console
        back and they will be printed out onto console instantly.
      example_readme:
      - examples/driver_examples/lpuart/polling_trigger/readme.md
      - examples/_boards/${board}/driver_examples/lpuart/polling_trigger/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm33_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release

# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
flexspi_hyper_flash_polling_transfer:
  section-type: application
  contents:
    meta_path: examples/driver_examples/flexspi/hyper_flash/polling_transfer
    project-root-path: boards/${board}/driver_examples/flexspi/hyper_flash/polling_transfer/${multicore_foldername}
    document:
      name: flexspi_hyper_flash_polling_transfer${core_id_suffix_name}
      category: driver_examples/flexspi
      brief: The flexspi_hyper_flash_polling_transfer example shows how to use flexspi
        driver with polling:In this example, flexspi will send data and operate the
        external Hyper flash connected with FLEXSPI. Some simple flash command willbe
        executed, such as Read ID, Erase Sector and Program Buffer.Example will first
        configures hyper flash to enter ASO mode, read ID-CFI parameters, then exit
        ASO mode.Second, the example erase a sector in flash, check if the erase is
        successful and program the same sector, then read backthe sector data. At
        last check if the data read back is correct.
      example_readme:
      - examples/driver_examples/flexspi/hyper_flash/polling_transfer/readme.md
      - examples/_boards/${board}/driver_examples/flexspi/hyper_flash/polling_transfer/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    evkbimxrt1050: []
    evkbmimxrt1060:
    - -armgcc@flexspi_nor_debug
    - -armgcc@flexspi_nor_release
    - -iar@flexspi_nor_debug
    - -iar@flexspi_nor_release
    - -mdk@flexspi_nor_debug
    - -mdk@flexspi_nor_release
    evkcmimxrt1060:
    - -armgcc@flexspi_nor_debug
    - -armgcc@flexspi_nor_release
    - -iar@flexspi_nor_debug
    - -iar@flexspi_nor_release
    - -mdk@flexspi_nor_debug
    - -mdk@flexspi_nor_release
    evkmimxrt1064: []

# yaml-language-server: $schema=../../../../scripts/data_schema/example_description_schema.json
easrc_p2p_out_sdma:
  section-type: application
  contents:
    meta_path: examples/driver_examples/easrc/asrc_p2p_out_sdma
    project-root-path: boards/${board}/driver_examples/asrc/asrc_p2p_out_sdma/${multicore_foldername}
    document:
      name: easrc_p2p_out_sdma${core_id_suffix_name}
      category: driver_examples/asrc
      brief: The asrc_p2p_out_sdma example shows how to use asrc driver with SDMA
        :In this example, one asrc instance convert audio data and push to sai directly.
      example_readme:
      - examples/driver_examples/easrc/asrc_p2p_out_sdma/readme.md
      - examples/_boards/${board}/driver_examples/asrc/asrc_p2p_out_sdma/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    evkmimx8mn:
    - -armgcc@debug
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    evkmimx8mnddr3l:
    - -armgcc@debug
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    evkmimx8mp:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release

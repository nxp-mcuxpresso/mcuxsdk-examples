# yaml-language-server: $schema=../../../../scripts/data_schema/example_description_schema.json
easrc_m2m_polling:
  section-type: application
  contents:
    meta_path: examples/driver_examples/easrc/asrc_m2m_polling
    project-root-path: boards/${board}/driver_examples/asrc/asrc_m2m_polling/${multicore_foldername}
    document:
      name: easrc_m2m_polling${core_id_suffix_name}
      category: driver_examples/asrc
      brief: The asrc_m2m_polling example shows how to use ASRC driver with polling:In
        this example, one ASRC instance convert audio data and store it in ram using
        polling.
      example_readme:
      - examples/driver_examples/easrc/asrc_m2m_polling/readme.md
      - examples/_boards/${board}/driver_examples/asrc/asrc_m2m_polling/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    evkmimx8mn:
    - -armgcc@debug
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    evkmimx8mnddr3l:
    - -armgcc@debug
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -armgcc@release
    - -iar@debug
    - -iar@flash_debug
    - -iar@flash_release
    - -iar@release
    evkmimx8mp:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release

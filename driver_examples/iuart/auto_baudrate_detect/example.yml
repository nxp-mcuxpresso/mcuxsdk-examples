# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
iuart_auto_baudrate_detect:
  section-type: application
  contents:
    meta_path: examples/driver_examples/iuart/auto_baudrate_detect
    project-root-path: boards/${board}/driver_examples/uart/auto_baudrate_detect/${multicore_foldername}
    document:
      name: iuart_auto_baudrate_detect${core_id_suffix_name}
      category: driver_examples/uart
      brief: The uart_auto_baudrate_detect example shows how to use uart auto baud
        rate detect feature:In this example, one uart instance connect to PC through
        uart. First, we should send characters a or A to board. The boars will set
        baud rate automatic. After baud rate has set, the board will send back all
        characters that PCsend to the board.
      example_readme:
      - examples/driver_examples/iuart/auto_baudrate_detect/readme.md
      - ${board_root}/${board}/driver_examples/uart/auto_baudrate_detect/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    evkmimx8mm:
    - -armgcc@ddr_debug
    - -armgcc@ddr_release
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@ddr_debug
    - -iar@ddr_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mn:
    - -armgcc@ddr_debug
    - -armgcc@ddr_release
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@ddr_debug
    - -iar@ddr_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mnddr3l:
    - -armgcc@ddr_debug
    - -armgcc@ddr_release
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@ddr_debug
    - -iar@ddr_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mp:
    - -armgcc@ddr_debug
    - -armgcc@ddr_release
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@ddr_debug
    - -iar@ddr_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mq:
    - -armgcc@ddr_debug
    - -armgcc@ddr_release
    - -iar@ddr_debug
    - -iar@ddr_release

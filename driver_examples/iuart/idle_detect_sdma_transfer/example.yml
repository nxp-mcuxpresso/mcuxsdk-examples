# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
iuart_idle_detect_sdma_transfer:
  section-type: application
  contents:
    meta_path: examples/driver_examples/iuart/idle_detect_sdma_transfer
    project-root-path: boards/${board}/driver_examples/uart/idle_detect_sdma_transfer/${multicore_foldername}
    document:
      name: iuart_idle_detect_sdma_transfer${core_id_suffix_name}
      category: driver_examples/uart
      brief: The uart_idle_detect_sdma example shows how to use uart driver in sdma
        way:In this example, one uart instance connect to PC through uart, the board
        willsend back all characters that PC send to the board.Uart will receive 8
        characters every time, but if the character is less then 8, the idleline interrupt
        will generate, and abort the SDMA receive operation, and send out the receivedcharacters.
      example_readme:
      - examples/driver_examples/iuart/idle_detect_sdma_transfer/readme.md
      - examples/_boards/${board}/driver_examples/uart/idle_detect_sdma_transfer/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    evkmimx8mm: []
    evkmimx8mn:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mnddr3l:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mp:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release

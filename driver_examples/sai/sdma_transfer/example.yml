# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
sai_sdma_transfer:
  section-type: application
  contents:
    meta_path: examples/driver_examples/sai/sdma_transfer
    project-root-path: boards/${board}/driver_examples/sai/sdma_transfer/${multicore_foldername}
    document:
      name: sai_sdma_transfer${core_id_suffix_name}
      category: driver_examples/sai
      brief: The sai_sdma_transfer example shows how to use sai driver with SDMA:In
        this example, one sai instance playbacks the audio data stored in flash/SRAM/DDR
        using SDMA channel.
      example_readme:
      - examples/driver_examples/sai/sdma_transfer/readme.md
      - examples/_boards/${board}/driver_examples/sai/sdma_transfer/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    evkmimx8mm:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mn:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mnddr3l:
    - -armgcc@flash_debug
    - -armgcc@flash_release
    - -iar@flash_debug
    - -iar@flash_release
    evkmimx8mp: []

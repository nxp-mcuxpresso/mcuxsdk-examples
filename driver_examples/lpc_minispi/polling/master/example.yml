# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
spi_polling_master:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpc_minispi/polling/master
    project-root-path: boards/${board}/driver_examples/spi/polling/master/${multicore_foldername}
    document:
      name: spi_polling_master${core_id_suffix_name}
      category: driver_examples/spi
      brief: 'The spi_polling_transfer_master example shows how to use spi driver
        as master to do board to boardtransfer with polling:In this example, one spi
        instance as master and another spi instance on othere board as slave. Mastersends
        a piece of data to slave, and receive a piece of data from slave. This example
        checks if the data received from slave is correct. This example need to work
        with spi_polling_transfer_slave example. '
      example_readme:
      - examples/driver_examples/lpc_minispi/polling/master/readme.md
      - examples/_boards/${board}/driver_examples/spi/polling/master/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    lpc845breakout: []
    lpcxpresso802: []
    lpcxpresso804: []
    lpcxpresso824max: []
    lpcxpresso845max: []
    lpcxpresso860max: []

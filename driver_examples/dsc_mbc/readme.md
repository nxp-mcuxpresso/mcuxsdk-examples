# dsc_mbc

## Overview
The dsc_mbc example shows how to control the memory block checker(MBC) access policy on DSC.

In this example, a MBC memory block are set to inaccessible, then the hardfault occurs.

## Supported Boards

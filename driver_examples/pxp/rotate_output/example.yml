# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
pxp_rotate_output:
  section-type: application
  contents:
    meta_path: examples/driver_examples/pxp/rotate_output
    project-root-path: boards/${board}/driver_examples/pxp/rotate_output/${multicore_foldername}
    document:
      name: pxp_rotate_output${core_id_suffix_name}
      category: driver_examples/pxp
      brief: The PXP picture rotation project demonstrates how to use the ROTATE0 module to implement 
        rotation functionality. If this exampleruns correctly, you will see a square with three 
        color(red, green and blue).The rotate degree is changing.
      example_readme:
      - examples/driver_examples/pxp/rotate_output/readme.md
      - ${board_root}/${board}/driver_examples/pxp/rotate_output/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    imx943evk@cm7_core1:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug
    imx943evk@cm7_core0:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug
    imx943evk@cm33_core1:
    - +armgcc@debug
    - +armgcc@release
    - +iar@release
    - +iar@debug

# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
romapi_nboot:
  section-type: application
  contents:
    meta_path: examples/driver_examples/romapi/nboot/.
    project-root-path: boards/${board}/driver_examples/romapi/nboot/${multicore_foldername}
    document:
      name: romapi_nboot${core_id_suffix_name}
      category: driver_examples/romapi/nboot
      brief: Rom API driver example for NBOOT interface.
      example_readme:
      - examples/driver_examples/romapi/nboot/readme.md
      - ${board_root}/${board}/driver_examples/romapi/nboot/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmrw612:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    rdrw612bga:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release

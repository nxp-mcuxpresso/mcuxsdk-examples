# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
romapi_bootloader:
  section-type: application
  contents:
    meta_path: examples/driver_examples/romapi/bootloader/.
    project-root-path: boards/${board}/driver_examples/romapi/bootloader/${multicore_foldername}
    document:
      name: romapi_bootloader${core_id_suffix_name}
      category: driver_examples/romapi/bootloader
      brief: Rom API driver example for bootloader functions.
      example_readme:
      - examples/driver_examples/romapi/bootloader/readme.md
      - ${board_root}/${board}/driver_examples/romapi/bootloader/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmrw612:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    mimxrt700evk@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    frdmimxrt700@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    rdrw612bga:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release

# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
romapi_otp:
  section-type: application
  contents:
    meta_path: examples/driver_examples/romapi/otp/.
    project-root-path: boards/${board}/driver_examples/romapi/otp/${multicore_foldername}
    document:
      name: romapi_otp${core_id_suffix_name}
      category: driver_examples/romapi/otp
      brief: Rom API driver example for OCOTP interface.
      example_readme:
      - examples/driver_examples/romapi/otp/readme.md
      - ${board_root}/${board}/driver_examples/romapi/otp/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmrw612:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    mimxrt700evk@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    frdmimxrt700@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    rdrw612bga:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release

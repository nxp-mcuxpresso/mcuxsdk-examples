# yaml-language-server: $schema=../../../../../../scripts/data_schema/example_description_schema.json
#
# Copyright 2024 NXP
#
# SPDX-License-Identifier: BSD-3-Clause
mmu_xspi:
  section-type: application
  contents:
    meta_path: examples/driver_examples/mmu/xspi
    project-root-path: boards/${board}/driver_examples/mmu/xspi/${multicore_foldername}
    document:
      name: mmu_xspi
      category: driver_examples
      brief: The mmu xspi example shows how to use MMU driver to configure MMU module.
      example_readme:
      - examples/driver_examples/mmu/xspi/readme.md
      - ${board_root}/${board}/driver_examples/mmu/xspi/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    mimxrt700evk@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    frdmimxrt700@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release

# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
gdma_transfer_basic:
  section-type: application
  contents:
    meta_path: examples/driver_examples/gdma/transfer_basic
    project-root-path: boards/${board}/driver_examples/gdma/transfer_basic/${multicore_foldername}
    document:
      name: gdma_transfer_basic${core_id_suffix_name}
      category: driver_examples/gdma
      brief: This example shows how to use GDMA transactional APIs to do memory to
        memory data copy.
      example_readme:
      - examples/driver_examples/gdma/transfer_basic/readme.md
      - ${board_root}/${board}/driver_examples/gdma/transfer_basic/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmrw612:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    rdrw612bga:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release

flexio_a-format_interrupt_transfer:
  section-type: application
  contents:
    meta_path: examples/driver_examples/flexio/a-format/interrupt_transfer
    project-root-path: boards/${board}/driver_examples/flexio/a-format/interrupt_transfer/${multicore_foldername}
    document:
      name: flexio_a-format_interrupt_transfer${core_id_suffix_name}
      category: driver_examples/flexio
      brief: The flexio_a-format_interrupt example shows how to use flexio A-format driver
        in interrupt way:In this example, a flexio simulated A-format connect to the encoder
        through RS-485, the board will send requests to and receive responses from the encoder.
      example_readme:
      - examples/driver_examples/flexio/a-format/interrupt_transfer/readme.md
      - ${board_root}/${board}/driver_examples/flexio/a-format/interrupt_transfer/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    evkmimxrt1180@cm33: []
    evkmimxrt1180@cm7: []
    imx943evk@cm7_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release
    imx943evk@cm7_core0:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release
    imx943evk@cm33_core1:
    - +iar@debug
    - +armgcc@debug
    - +iar@release
    - +armgcc@release

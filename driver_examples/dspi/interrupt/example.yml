# yaml-language-server: $schema=../../../../../scripts/data_schema/example_description_schema.json
dspi_interrupt:
  section-type: application
  contents:
    meta_path: examples/driver_examples/dspi/interrupt
    project-root-path: boards/${board}/driver_examples/dspi/interrupt/${multicore_foldername}
    document:
      name: dspi_interrupt${core_id_suffix_name}
      category: driver_examples/dspi
      brief: The dspi_interrupt example shows how to use DSPI driver in interrupt
        way:In this example , one dspi instance used as DSPI master and another dspi
        instance used as DSPI slave in the same board.This example does not use the
        transactional API in DSPI driver. It's a demonstration that how to use the
        interrupt in KSDK driver.1. DSPI master send/received data to/from DSPI slave
        in interrupt . (DSPI Slave using interrupt to receive/send the data)
      example_readme:
      - examples/driver_examples/dspi/interrupt/readme.md
      - examples/_boards/${board}/driver_examples/dspi/interrupt/example_board_readme.md
      - examples/_boards/${board}/examples_shared_readme.md
  boards:
    frdmk22f: []

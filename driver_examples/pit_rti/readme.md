# pit

## Overview
The PIT RTI project is a simple demonstration program of the RTI timer in SDK PIT driver. It sets up the RTI timer to trigger a periodic interrupt after every 1 second. When the PIT RTI interrupt is triggered, a message will be printed on the UART terminal and an LED toggled on the board.

## Supported Boards


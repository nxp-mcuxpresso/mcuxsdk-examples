tflm_label_image_ext_mem:
  section-type: application
  contents:
    meta_path: examples/eiq_examples/tflm_label_image_ext_mem/.
    project-root-path: boards/${board}/eiq_examples/tflm_label_image_ext_mem/${core_id}
    document:
      name: tflm_label_image_ext_mem${core_id_suffix_name}
      category: eiq_examples
      brief: Label image external memory example for TensorFlow Lite Micro
      example_readme:
      - examples/eiq_examples/tflm_label_image_ext_mem/readme.md
      - ${board_root}/${board}/eiq_examples/tflm_label_image_ext_mem/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    mimxrt700evk@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - +iar@flash_debug
    - +iar@flash_release
    - +mdk@flash_debug
    - +mdk@flash_release
    mimxrt2660evk:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    - -armgcc@psram_debug
    - -armgcc@psram_release
    - -iar@psram_debug
    - -iar@psram_release
    - -mdk@psram_debug
    - -mdk@psram_release
    - -armgcc@psram_txt_debug
    - -armgcc@psram_txt_release
    - -iar@psram_txt_debug
    - -iar@psram_txt_release
    - -mdk@psram_txt_debug
    - -mdk@psram_txt_release

# yaml-language-server: $schema=../../../../scripts/data_schema/example_description_schema.json
decompress_etc2_freertos:
  section-type: application
  contents:
    meta_path: examples/vglite_examples/decompress_etc2_freertos
    project-root-path: boards/${board}/vglite_examples/decompress_etc2_freertos/${core_id}
    document:
      name: decompress_etc2_freertos${core_id_suffix_name}
      category: vglite_examples
      brief: This example shows how to use the vglite component. This application
        draws a tiger on the screen.
      example_readme:
      - examples/vglite_examples/decompress_etc2_freertos/readme.md
      - ${board_root}/${board}/vglite_examples/decompress_etc2_freertos/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    mimxrt700evk@cm33_core0:
    - +armgcc@debug
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - +armgcc@release
    - +iar@debug
    - +iar@flash_debug
    - +iar@flash_release
    - +iar@release
    - +mdk@debug
    - +mdk@flash_debug
    - +mdk@flash_release
    - +mdk@release
    frdmimxrt700@cm33_core0:
    - +armgcc@debug
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - +armgcc@release
    - +iar@debug
    - +iar@flash_debug
    - +iar@flash_release
    - +iar@release
    - +mdk@debug
    - +mdk@flash_debug
    - +mdk@flash_release
    - +mdk@release

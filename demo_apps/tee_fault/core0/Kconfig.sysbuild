
config secondary_board
    string
    default "$(board)"

config secondary_toolchain
    string
    default "$(toolchain)"

source "${SdkRootDirPath}/${board_root}/${board}/demo_apps/tee_fault/core0/Kconfig.sysbuild"